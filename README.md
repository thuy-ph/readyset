# readyset

**From reported to ready.** Readyset is an intake/clarification layer for commercial property facilities teams. It sits in front of the work-order system you already use: reads maintenance requests from every channel (email, phone, SMS, portal), extracts the fields that stall a job, asks the requester one clarifying question, flags likely duplicates without merging them, and hands a human a dispatch-ready work order.

**Hero metric — Time-to-Ready: 2.4 days → 4 minutes.**

Hackathon prototype. All demo data is simulated and fictional.

## Live site

- `/` — landing page (full brand build: hero, live email walkthrough, FAQ, founders, demo entry points)
- `/demo/` — interactive coordinator console demo of the core loop: multi-channel intake → AI field extraction → **auto ask-back** → **duplicate detection** (suggested, human-confirmed) → dispatch-ready work order
- `/brand/` — the Brand Identity System deck as a native site page
- `/deck/` — pitch deck: 9-slide web viewer with keyboard nav, plus downloadable `.pptx`
- `/reel/` — campaign reel page (shows the video once `reel/reel.mp4` is dropped in; placeholder until then)

## Repo map (read this before pushing)

| Path | What it is | Notes |
|---|---|---|
| `index.html` | Landing page, served at `/` | **Generated** — a copy of `landing-page/dist/index.html`. Don't hand-edit; edit `landing-page/src/` and rebuild |
| `landing-page/` | Landing page source + build | `node src/build.mjs` inlines fonts/GSAP/images into one self-contained file. See `landing-page/README.md` |
| `demo/` | Coordinator console demo | Scripted scene engine — don't edit without syncing |
| `brand/index.html` | Brand system page | Generated by `landing-page/src/build-brand.mjs` |
| `deck/` | Pitch deck viewer + `readyset-pitch.pptx` | |
| `reel/` | Reel page + assets | Page is generated from `landing-page/src/reel.html`; publishing steps in `reel/README.md` |
| `PRODUCT.md`, `DESIGN.md` | Shared brand + product context | Palette, fonts and voice live here — use them everywhere |
| `customer-persona.md` | Aisha Rahman (buyer persona), with Leo as daily-user sketch | |
| `reel-brief.md` | Brief for the promo video | |

No install step anywhere. The site is plain HTML/CSS/JS served as-is by GitHub Pages — push to `main` and it's live. The only build is the landing/brand/reel pages: `node landing-page/src/build.mjs` (Node 18+, zero dependencies), then copy `dist/` output to the served paths per `landing-page/README.md`.

## Brand quick reference

- Palette: Ink `#0B211B` · Spruce `#14382E` · Field `#5E7A6D` · Signal `#2FD284` (only for "ready"/CTA) · Paper `#F5F2E9`
- Type: Space Grotesk (headlines) · Inter (body) · IBM Plex Mono (IDs, labels, tags)
- Voice: precise, unhurried, credible. "The work order is ready. You just approve it."

## Product guardrails (also true of the demo)

1. Integrates with the existing work-order system — never replaces it.
2. Emergencies are handed to a human immediately; the agent only spots them fast.
3. Duplicate merges are suggested, never automatic.
4. Every write to the system of record is a human approval click.

## Content accuracy

Two competitive claims on the landing page were checked against live products and are worded carefully — don't loosen them (details in `landing-page/README.md`): we don't claim nobody reads inbound email (Re-Leased does, and the page names it); the defensible claim is **cross-channel duplicate reconciliation**.
